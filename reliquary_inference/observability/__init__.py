"""Observability primitives for reliquary-inference.

Houses the OpenTelemetry tracing facade (``tracing``). Metrics modules
live alongside the component that emits them (e.g.
``validator.metrics``, ``validator.mesh_observability``).
"""
